<div class = 'message'>
<div class = 'body' >
	<p id = 'c-body' >
    <%= comment.body%>
  </p>
  <p>
	  Posted by:<%= comment.user.name.capitalize %>
  </p>
</div>

  <%= link_to 'Edit', edit_article_comment_path(comment.article, comment) %> |
  <%= link_to 'Delete', [comment.article, comment], method: :delete,
    data: { confirm: 'Are you sure?' } %>
</div>
