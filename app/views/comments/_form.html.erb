<%= form_for([@article, @comment]) do |f| %>
<%= render "shared/error_messages", object: @comment %>

  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body, size: "40x15" %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>